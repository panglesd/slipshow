========
GUI mode
========

Slipshow has a "GUI mode". Add the ``gui`` attribute to any element. Then select
it either by putting the editor cursor on ``gui``, or by using the preview
selector: :key:`Shift + G` and then click on the element.
