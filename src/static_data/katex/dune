(library
 (name katex)
 (public_name slipshow.katex))

(rule
 (aliases katex)
 (deps
  (:x katex.ml)
  (glob_files_rec data/*)
 )
 (action
  (progn
   (setenv
    SOURCE_DATE_EPOCH
    0
    (run ocaml-crunch data/ -o %{x}.corrected -m plain -s))
   (diff? %{x} %{x}.corrected))))
